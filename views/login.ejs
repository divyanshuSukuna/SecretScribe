<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SecretScribe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="text-blue-500 text-sm mb-10">
            <a href="/home">Go to Home Page</a>
        </div>
        <h1 class="text-3xl mb-5">Login to your Account</h1>

        <!-- Error Display -->
        <% if (errors && errors.length > 0) { %>
            <ul class="mb-5 text-red-500">
                <% errors.forEach(function(error) { %>
                    <li><%= error %></li>
                <% }); %>
            </ul>
        <% } %>

        <!-- Form -->
        <form action="/login" method="post"> 
            <input class="px-3 py-2 bg-zinc-800 border-2 bg-transparent rounded-md border-zinc-700"  type="email" placeholder="Email" name="email">
            <input class="px-3 py-2 bg-zinc-800 border-2 bg-transparent rounded-md border-zinc-700"  type="password" placeholder="Password" name="password">
            <input class="px-5 py-2 bg-blue-500 border-2 rounded-md border-zinc-700"  type="submit" value="Login">
        </form>
    </div>
</body>
</html>