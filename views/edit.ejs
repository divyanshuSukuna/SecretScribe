<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SecretScribe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end text-sm mb-10">
            <a class=" bg-red-500 inline-block rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class=" w-1/3 rounded-md px-3 py-2 block bg-transparent bg-zinc-800 resize-none" name="content"><%=post.content%></textarea>
            <input class="bg-yellow-600 rounded-md px-3 py-2 mt-3 block" type="submit" value="Update Post">
        </form>

    </div>
</body>
</html>